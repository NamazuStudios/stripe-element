name: Snapshot Build

on:
  push:
    branches: [ main, "maintenance/*" ]
  workflow_dispatch:

concurrency:
  group: stripe-element-integration
  cancel-in-progress: false

jobs:

  snapshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21 and Maven Credentials
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Get current version
        id: version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current project version: $VERSION"

      - name: Ensure Snapshot Version
        run: |
          CURRENT=${{ steps.version.outputs.version }}
          if [[ "$CURRENT" != *-SNAPSHOT ]]; then
            echo "::error::Expected a -SNAPSHOT version, but found '$CURRENT'."
            echo "The main branch must end with -SNAPSHOT before running a snapshot build."
            echo "Please bump the version in pom.xml and push before retrying."
            exit 1
          fi

      - name: Build and Publish Snapshot
        run: mvn -B --no-transfer-progress -Pgithub-publish clean deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
