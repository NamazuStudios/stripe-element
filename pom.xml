<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>dev.getelements.elements.stripe</groupId>
    <artifactId>stripe</artifactId>
    <version>1.0.4-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Stripe Element</name>
    <description>Namazu Elements integration for Stripe payment processing</description>
    <url>https://github.com/NamazuStudios/stripe-element</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://github.com/NamazuStudios/stripe-element/blob/main/LICENSE</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:git@github.com:NamazuStudios/stripe-element.git</connection>
        <developerConnection>scm:git:git@github.com:NamazuStudios/stripe-element.git</developerConnection>
        <url>https://github.com/NamazuStudios/stripe-element</url>
    </scm>

    <developers>
        <developer>
            <name>Namazu Studios</name>
            <organization>Namazu Studios LLC</organization>
            <organizationUrl>https://namazustudios.com</organizationUrl>
        </developer>
    </developers>

    <modules>
        <module>api</module>
        <module>ui</module>
        <module>element</module>
        <module>debug</module>
        <module>integration-test</module>
    </modules>

    <properties>
        <encoding>UTF-8</encoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <elements.version>3.8.16</elements.version>
        <swagger.version>2.2.22</swagger.version>
        <guice.version>7.0.0</guice.version>
        <rs.api>4.0.0</rs.api>
        <jakarta.websocket.version>2.1.0</jakarta.websocket.version>
        <crossfire.version>1.0.4</crossfire.version>
        <servlet.api>6.1.0</servlet.api>
        <logback.version>1.2.3</logback.version>
        <stripe.java.version>25.3.0</stripe.java.version>
        <morphia.version>2.4.14</morphia.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.build.timestamp.format>yyyy-MM-dd'T'HH:mm:ss'Z'</maven.build.timestamp.format>
        <api.classifier>${project.groupId}.api</api.classifier>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.4.2</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>3.3.1</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>3.6.3</version>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- Source Jars - generated in all builds so IDEs, dependents, and Maven Central have sources available -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <!-- Javadoc Jars - generated in all builds so IDEs, dependents, and Maven Central have documentation available -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <!--
        Everything in dependencyManagement serves the purpose of centralizing the versions and configurations of
        artifacts used. Every project will refer to this section when including them on the classpath. This is
        configured this way to deliberately avoid confusion over what should be scoped properly.
    -->
    <dependencyManagement>
        <dependencies>

            <!--
                The BOM for Namazu Elements incorporates all the official SDK packages. The packages are provided
                with correct scope and version matching the requested version.
            -->

            <dependency>
                <groupId>dev.getelements.elements</groupId>
                <artifactId>sdk-bom</artifactId>
                <version>${elements.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
                The Element's API and Implementation. Note that the API is provided meaning that it will not be packaged
                directly with the Element. Rather it will be added to the bundle as an API dependency. This is important
                when exporting the Element's interfaces to other Elements within the system.
            -->

            <dependency>
                <groupId>dev.getelements.elements.stripe</groupId>
                <artifactId>api</artifactId>
                <version>${project.version}</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>dev.getelements.elements.stripe</groupId>
                <artifactId>api</artifactId>
                <version>${project.version}</version>
                <classifier>${api.classifier}</classifier>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>dev.getelements.elements.stripe</groupId>
                <artifactId>element</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Stripe Java SDK - bundled inside the .elm (compile scope, not provided) -->
            <dependency>
                <groupId>com.stripe</groupId>
                <artifactId>stripe-java</artifactId>
                <version>${stripe.java.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <snapshotRepository>
            <id>ossrh</id>
            <url>https://s01.oss.sonatype.org/content/repositories/snapshots/</url>
        </snapshotRepository>
        <repository>
            <id>ossrh</id>
            <url>https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/</url>
        </repository>
    </distributionManagement>

    <profiles>

        <profile>
            <id>github-publish</id>
            <distributionManagement>
                <snapshotRepository>
                    <id>github</id>
                    <url>https://maven.pkg.github.com/NamazuStudios/stripe-element</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>

        <profile>
            <id>central-publish</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                            <waitMaxTime>3600</waitMaxTime>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.4</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>

</project>
