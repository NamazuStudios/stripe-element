<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.getelements.elements.stripe</groupId>
        <artifactId>stripe</artifactId>
        <version>1.0.5</version>
    </parent>

    <artifactId>integration-test</artifactId>
    <version>1.0.5</version>
    <name>Stripe Element Integration Tests</name>
    <url>https://github.com/NamazuStudios/stripe-element</url>

    <properties>
        <!-- Defaults sourced from CI-friendly env vars; override with -Dstripe.test.* for local runs. -->
        <stripe.test.apiKey>${env.STRIPE_TEST_API_KEY}</stripe.test.apiKey>
        <stripe.test.customerId>${env.STRIPE_TEST_CUSTOMER_ID}</stripe.test.customerId>
        <stripe.test.priceId>${env.STRIPE_TEST_PRICE_ID}</stripe.test.priceId>
    </properties>

    <dependencies>
        <dependency>
            <groupId>dev.getelements.elements.stripe</groupId>
            <artifactId>element</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>dev.getelements.elements.stripe</groupId>
            <artifactId>api</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.stripe</groupId>
            <artifactId>stripe-java</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>dev.getelements.elements</groupId>
            <artifactId>sdk-dao</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.getelements.elements</groupId>
            <artifactId>sdk-model</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.getelements.elements</groupId>
            <artifactId>sdk-service</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>5.11.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>5.11.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.ws.rs</groupId>
            <artifactId>jakarta.ws.rs-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.core</groupId>
            <artifactId>jersey-server</artifactId>
            <version>3.1.9</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.2.5</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <systemPropertyVariables>
                        <stripe.test.apiKey>${stripe.test.apiKey}</stripe.test.apiKey>
                        <stripe.test.customerId>${stripe.test.customerId}</stripe.test.customerId>
                        <stripe.test.priceId>${stripe.test.priceId}</stripe.test.priceId>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- Skip unit tests in this module - only ITs run here -->
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
